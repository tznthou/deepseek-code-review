# ── 憑證：這個 kit 需要 DEEPSEEK_API_KEY，別讓它落地 ──
# README 教的是 export，但實務上很多人會寫進 .env——2026-09-20 實測：
# 不寫這幾條，`.env` 與 `*.key` 會被 git add -A 直接收進去。
.env
.env.*
!.env.example
*.key
*.pem

# ── Claude Code 的本機設定與私有 skill，絕不進 repo ──
.claude/

# ── macOS ──
.DS_Store

# 本機執行 kit 時可能產生的檔案（不要進版控）
__pycache__/
*.py[cod]

# review-local.sh 預設寫在 /tmp，但手動改路徑時可能落在這裡
pr.diff
review.md
findings.json
dsh-stderr.log
body.md
dsh-composed-config.yml

# dsh 的隔離 home（workflow 會設 DSH_HOME 到這裡）
.dsh-home/
